/* General Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Arial, sans-serif;
    scroll-behavior: smooth;
}

/* Body */
body {
    background-color: #f4f4f4;
    color: #333;
    line-height: 1.6;
}

/* Navigation Bar */
.navbar {
    background: #222;
    color: white;
    padding: 15px 30px;
    position: fixed;
    top: 0;
    width: 100%;
    z-index: 1000;
    display: flex;
    justify-content: center;
    align-items: center;
}

.nav-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    max-width: 1200px;
}

.navbar .logo {
    font-size: 24px;
    font-weight: bold;
    margin-bottom: 10px;
}

.nav-links {
    list-style: none;
    display: flex;
    justify-content: center;
    align-items: center;
}

.nav-links li {
    margin: 0 15px;
}

.nav-links a {
    color: white;
    text-decoration: none;
    font-size: 18px;
    transition: color 0.3s ease;
}

.nav-links a:hover {
    color: #f4a261;
}

/* Mobile Menu */
.mobile-menu {
    display: none;
    font-size: 24px;
    cursor: pointer;
}

/* Banner Section */
.banner {
    position: relative;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: white;
    overflow: hidden;
}

#banner-video {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transform: translate(-50%, -50%);
    z-index: -2;
}

.banner-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    z-index: -1;
}

.banner-content {
    position: relative;
    z-index: 1;
    background: rgba(0, 0, 0, 0.6);
    padding: 20px;
    border-radius: 10px;
    text-align: center;
}

.banner-content h1 {
    font-size: 50px;
    margin-bottom: 10px;
    white-space: pre-line;
}

.banner-content p {
    font-size: 24px;
    margin-top: 10px;
    white-space: pre-line;
}

/* Content Sections */
.content-section {
    padding: 60px 10%;
    text-align: center;
    background-color: white;
    margin-top: 20px;
}

.section-title {
    font-size: 32px;
    margin-bottom: 20px;
    color: #222;
}

/* About Section */
.about-content {
    max-width: 800px;
    margin: 0 auto;
    font-size: 18px;
    line-height: 1.6;
    color: #555;
}

/* Work Gallery */
.work-gallery {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 15px;
    margin-top: 30px;
}

.work-item img {
    width: 100%;
    height: auto;
    border-radius: 10px;
    transition: transform 0.3s ease;
}

.work-item img:hover {
    transform: scale(1.05);
}

/* Skills Section */
.skills-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
}

.skill-item {
    background: #f4a261;
    color: white;
    padding: 15px;
    border-radius: 8px;
    max-width: 300px;
    text-align: center;
    transition: transform 0.3s ease;
}

.skill-item:hover {
    transform: scale(1.05);
}

/* Work Experiences Tree Structure (Adjusted) */
.work-container {
    display: flex;
    justify-content: space-around;
    margin-top: 30px;
    flex-wrap: wrap; /* Allow wrapping on smaller screens */
}

.work-column {
    width: 24%; /* Adjust as needed, consider responsiveness */
    min-width: 250px; /* Ensure a minimum width */
    text-align: left; /* Align text to the left */
    margin-bottom: 20px;
    display: flex; /* Use flexbox for height consistency */
    flex-direction: column; /* Stack content vertically */
}

.work-column h3 {
    margin-bottom: 10px;
    font-size: 1.2em; /* Consistent font size */
    text-align: center; /* Center the company name */
}

.job-experience {
    border: 1px solid #ddd;
    padding: 15px;
    border-radius: 8px;
    display: flex;
    flex-direction: column;
    min-height: 350px; /* Adjusted min-height */
    background-color: #e0f2fe; /* Light blue background */
    transition: background-color 0.3s ease, transform 0.3s ease; /* Add transition for hover effect */
}

.job-experience:hover {
    background-color: #cce0f5; /* Slightly darker blue on hover */
    transform: translateY(-5px); /* Move box slightly up on hover */
}

.job-experience img {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    margin-bottom: 10px;
    align-self: center; /* Center image */
}

.job-experience h4 {
    margin-bottom: 5px;
    font-size: 1em; /* Consistent font size */
    text-align: center; /* Center the job title */
}

.job-experience p {
    font-size: 0.9em; /* Consistent font size */
    margin-bottom: 10px;
    text-align: center; /* Center the job title */
}

.roles-responsibilities {
    display: flex;
    flex-direction: column;
}

.roles-responsibilities h5 {
    font-size: 0.9em; /* Consistent font size */
    margin-bottom: 5px;
}

.roles-responsibilities ul {
    list-style-type: disc;
    padding-left: 20px;
    font-size: 0.9em; /* Consistent font size */
}

.roles-responsibilities li {
    margin-bottom: 5px;
}

/* Contact Section */
.contact-links {
    display: flex;
    justify-content: center;
    margin-bottom: 20px;
}

.contact-links a {
    margin: 0 15px;
}

.contact-icon img {
    width: 100%; /* Make image fill the container */
    height: 100%; /* Make image fill the container */
    object-fit: contain; /* Prevents distortion */
}

.contact-icon {
    width: 40px; /* Set a fixed width */
    height: 40px; /* Set a fixed height */
    display: inline-flex; /* Use flexbox for centering */
    justify-content: center; /* Center horizontally */
    align-items: center; /* Center vertically */
    border-radius: 50%;
    background-color: #f0f0f0;
    padding: 0; /* Remove padding */
    transition: background-color 0.3s ease;
}

.contact-icon:hover {
    background-color: #f4a261;
    transform: scale(1.1);
}

.contact-form {
    max-width: 500px;
    margin: 0 auto;
}

.form-group {
    margin-bottom: 15px;
}

.contact-form input,
.contact-form textarea {
    width: 100%;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
}

.contact-form button {
    width: 100%;
    padding: 10px;
    background: #222;
    color: white;
    border: none;
    cursor: pointer;
    transition: background 0.3s ease;
}

.contact-form button:hover {
    background: #f4a261;
}

/* Footer */
footer {
    background: #222;
    color: white;
    text-align: center;
    padding: 15px;
    margin-top: 20px;
}

/* Responsive Design */
@media (max-width: 768px) {
    .navbar {
        flex-direction: column;
        align-items: flex-start;
    }

    .nav-links {
        display: none;
        flex-direction: column;
        width: 100%;
    }

    .nav-links li {
        margin: 10px 0;
    }

    .mobile-menu {
        display: block;
    }

    .banner-content {
        padding: 15px;
    }

    .banner-content h1 {
        font-size: 32px;
    }

    .banner-content p {
        font-size: 18px;
    }

    .skills-container {
        flex-direction: column;
        align-items: center;
    }

    .work-container {
        flex-direction: column;
    }
